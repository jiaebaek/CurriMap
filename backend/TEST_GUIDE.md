# 백엔드 테스트 가이드

백엔드 서버가 제대로 동작하는지 테스트하는 방법입니다.

## 🚀 빠른 시작

### 1. 백엔드 서버 실행

```bash
cd backend
npm run dev
```

서버가 시작되면 다음과 같은 메시지가 표시됩니다:

```
================================================================================
🚀 백엔드 서버가 시작되었습니다!
================================================================================
📍 Port: 3000
📝 Environment: development
🌐 Health Check: http://localhost:3000/health
🔗 API Base URL: http://localhost:3000/api
================================================================================
💡 모든 요청과 응답이 로깅됩니다.
```

### 2. 자동 테스트 실행

**새 터미널 창**을 열고:

```bash
cd backend
npm test
```

또는:

```bash
node test-server.js
```

이 스크립트는 다음을 테스트합니다:
- ✅ Health Check 엔드포인트
- ✅ 404 에러 처리
- ✅ 인증 에러 (401)
- ✅ Validation 에러 (400)

## 🧪 수동 테스트 방법

### 방법 1: 브라우저에서 테스트

브라우저에서 다음 URL을 열어보세요:

```
http://localhost:3000/health
```

다음과 같은 응답이 보여야 합니다:

```json
{
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

### 방법 2: curl 명령어 사용

**PowerShell**에서:

```powershell
# Health Check
curl http://localhost:3000/health

# 404 테스트
curl http://localhost:3000/api/nonexistent

# 인증 에러 테스트 (401 예상)
curl http://localhost:3000/api/children

# POST 요청 테스트
curl -X POST http://localhost:3000/api/children `
  -H "Content-Type: application/json" `
  -d '{\"nickname\":\"\",\"birth_months\":-1}'
```

### 방법 3: Postman 사용

1. Postman을 열고 새 요청 생성
2. Method: `GET`
3. URL: `http://localhost:3000/health`
4. Send 버튼 클릭

## 📋 로그 확인

백엔드 서버가 실행 중인 터미널에서 모든 요청과 응답이 로깅됩니다:

```
================================================================================
[2024-01-01T12:00:00.000Z] 📥 INCOMING REQUEST
Method: GET
URL: /health
Path: /health
User ID: anonymous
IP: ::1
================================================================================

================================================================================
[2024-01-01T12:00:00.123Z] 📤 OUTGOING RESPONSE
Status: 200
Response Time: 123ms
Response Body: {
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z"
}
================================================================================
```

## ❌ 문제 해결

### 로그가 안 찍힐 때

1. **서버가 실행 중인지 확인**
   ```bash
   # 포트가 사용 중인지 확인 (PowerShell)
   netstat -ano | findstr :3000
   ```

2. **서버 재시작**
   - `Ctrl + C`로 서버 중지
   - `npm run dev`로 다시 시작

3. **환경 변수 확인**
   - `.env` 파일이 `backend` 폴더에 있는지 확인
   - Supabase 설정이 올바른지 확인

### 연결 오류가 발생할 때

1. **포트 확인**
   - 기본 포트는 3000입니다
   - 다른 포트를 사용하는 경우 `.env` 파일에서 `PORT` 확인

2. **방화벽 확인**
   - Windows 방화벽이 포트를 차단하지 않는지 확인

3. **다른 서비스 확인**
   - 포트 3000이 다른 프로그램에서 사용 중인지 확인

## 🔍 추가 디버깅

### 상세한 에러 로그 보기

서버 실행 시 모든 에러가 상세하게 로깅됩니다:

```
⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠
[2024-01-01T12:00:00.000Z] ❌ ERROR OCCURRED
Method: POST
URL: /api/children
User ID: anonymous
Error Name: TypeError
Error Message: Cannot read property 'id' of undefined
Stack Trace: ...
⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠⚠
```

### 특정 엔드포인트 테스트

프론트엔드에서 요청을 보낼 때 백엔드 콘솔에서 로그를 확인하세요.


