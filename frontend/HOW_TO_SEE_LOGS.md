# 로그 확인 방법 (간단 가이드)

## Metro 콘솔이란?

**Metro**는 React Native/Expo 앱의 JavaScript 번들러입니다. 
앱을 실행할 때 `npm start` 명령어를 실행하면 나타나는 **터미널 창**이 바로 Metro 콘솔입니다.

## 📱 로그를 보는 방법

### 1단계: 프론트엔드 앱 실행

```bash
cd frontend
npm start
```

이 명령어를 실행하면 터미널에 다음과 같은 화면이 나타납니다:

```
› Metro waiting on exp://192.168.0.1:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)

› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web

› Press r │ reload app
› Press m │ toggle menu
```

**이 터미널 창이 바로 Metro 콘솔입니다!**

### 2단계: 로그 확인

앱을 실행하고 API 요청을 하면, **같은 터미널 창**에 다음과 같은 로그가 나타납니다:

```
================================================================================
📤 [API] OUTGOING REQUEST
Method: POST
URL: http://localhost:3000/api/children
...
================================================================================
```

## 🖥️ 실제 화면 예시

터미널에서 다음과 같이 보입니다:

```
$ npm start

> currimap-app@1.0.0 start
> expo start

Starting Metro Bundler...

================================================================================
📤 [API] OUTGOING REQUEST
Method: POST
URL: http://localhost:3000/api/children
Endpoint: /children
Body: {
  "nickname": "테스트",
  "birth_months": 36
}
Has Token: Yes
================================================================================

================================================================================
📥 [API] INCOMING RESPONSE
Status: 201 Created
Response Time: 123ms
✅ Success Response: {
  "data": {
    "id": 1,
    ...
  }
}
================================================================================
```

## 💡 팁

1. **터미널 창을 항상 열어두세요**
   - 앱을 실행한 터미널이 로그를 보여주는 창입니다
   - 이 창을 닫으면 로그를 볼 수 없습니다

2. **스크롤을 확인하세요**
   - 로그가 많으면 위로 스크롤해야 이전 로그를 볼 수 있습니다

3. **에러는 빨간색으로 표시됩니다**
   - `console.error()`로 출력된 내용은 빨간색으로 보입니다

4. **앱을 재시작하면 로그가 초기화됩니다**
   - `r` 키를 눌러 앱을 리로드하면 새로운 로그가 시작됩니다

## 🔍 다른 방법으로 로그 보기

### 방법 1: 브라우저 DevTools (웹에서 실행 시)

```bash
npm run web
```

브라우저가 열리면:
- `F12` 또는 `우클릭 → 검사` 클릭
- **Console** 탭에서 로그 확인

### 방법 2: React Native Debugger

앱 실행 중:
- **iOS**: `Cmd + D` (Mac) → "Debug" 선택
- **Android**: `Cmd + M` (Mac) 또는 `Ctrl + M` (Windows) → "Debug" 선택

Chrome DevTools가 열리면 Console 탭에서 로그 확인

### 방법 3: Expo Dev Tools

`npm start` 실행 시 자동으로 브라우저에 열립니다:
- `http://localhost:19000` (또는 표시된 포트)
- 여기서도 로그를 확인할 수 있습니다

## ❓ 자주 묻는 질문

**Q: Metro 콘솔이 안 보여요**
- A: `npm start`를 실행한 터미널 창을 확인하세요. 다른 터미널이 아닙니다!

**Q: 로그가 너무 많아요**
- A: `Ctrl + C`로 필터링하거나, 터미널을 새로 열어서 다시 시작하세요

**Q: 에러만 보고 싶어요**
- A: 터미널에서 `❌` 또는 `ERROR` 키워드로 검색하세요 (일부 터미널에서 지원)

**Q: 로그를 파일로 저장하고 싶어요**
- A: 터미널에서 출력을 파일로 리다이렉트:
  ```bash
  npm start > logs.txt 2>&1
  ```


